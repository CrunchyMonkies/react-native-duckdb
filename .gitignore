# Dependencies
node_modules/
bun.lock

# Build output
lib/
build/
.cxx/
.gradle/
Pods/
*.xcworkspace/

# DuckDB build artifacts (generated at build time)
duckdb/build-*/
duckdb/.extensions-configured
duckdb/extension/extension_config_local.cmake
package/DuckDB.xcframework/
# Package-local DuckDB clone (npm source-build fallback) + its build/config output
package/duckdb/

# Cross-compiled dependencies (OpenSSL, libcurl for httpfs) — build artifacts only.
# Keep the tracked vendor/duckdb markers (DUCKDB_VERSION) so they ship in the npm tarball.
vendor/openssl/
vendor/curl/

# iOS
example/ios/Podfile.lock
example/ios/Pods/
example/ios/build/
example/ios/.xcode.env.local
DerivedData/

# Android
example/android/.gradle/
example/android/app/build/
example/android/build/
example/android/app/release.keystore
example/android/keystore.properties
local.properties

# Misc
.DS_Store
*.tsbuildinfo
*.log
